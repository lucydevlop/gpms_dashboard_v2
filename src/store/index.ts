import { layoutStore } from './layoutStore';
import { userStore } from './userStore';
import { localeStore } from './localeStore';
import { parkinglotStore } from './parkinglotStore';

export const Store = {
  layoutStore,
  userStore,
  localeStore,
  parkinglotStore
};
